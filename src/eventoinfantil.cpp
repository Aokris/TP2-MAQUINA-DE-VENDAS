#include "eventoinfantil.h"
#include <iostream>
#include <string>

EventoInfantil::EventoInfantil(int id, string nome, int id_dono, int *capacidade, int *precos):
    Evento(id, nome, id_dono, capacidade, precos){
}